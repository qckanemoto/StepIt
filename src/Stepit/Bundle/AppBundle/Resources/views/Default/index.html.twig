{% extends 'StepitAppBundle::layout.html.twig' %}

{% block page_header %}
    <ol class="breadcrumb">
        <li class="active">{{ 'project.list' | trans }}</li>
    </ol>
{% endblock %}

{% block content_row %}
    <table class="table table-hover">
        <thead>
        <tr>
            <th>{{ 'project.name' | trans }}</th>
            <th>{{ 'project.description' | trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for project in projects %}
            <tr>
                <td><a href="{{ path('stepit_app_default_project', { project: project.id }) }}">{{ project.name }}</a></td>
                <td>{{ project.description }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
